package p2Interfaces;
// interfaces assume that all methods are public, abstract methods. There are exceptions.
// interfaces are abstract classes without any concrete methods.
public interface InterfaceCalculator {
	int square(int number);
}
